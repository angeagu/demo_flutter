import 'package:flutter/material.dart';

class GestionAlumnosView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Gestion Alumnos"),
    );
  }

}